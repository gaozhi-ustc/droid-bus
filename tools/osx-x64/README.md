# tools/osx-x64/(占位,本期不实现)

macOS Intel x64 二进制(`brew install android-platform-tools scrcpy` 或手工放置):

- `adb`、`scrcpy`、`scrcpy-server`、`sndcpy.apk`、`Adbkeyboard.apk`

窗口嵌入(`MacEmbedder`)尚未实现;当前 macOS 上 App 走缩略图降级。
