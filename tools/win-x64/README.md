# tools/win-x64/

从原「安卓投屏」`C:\Program Files (x86)\Androidscreen\Resources\` 拷贝整套:

- `adb.exe`、`scrcpy.exe`、`scrcpy-server`
- FFmpeg(`avcodec/avformat/avutil-*.dll`)、`SDL2.dll`
- `sndcpy.apk`(原在 Resources 父目录)、`Adbkeyboard.apk`

记录版本:scrcpy `2.x`,adb `____`。
